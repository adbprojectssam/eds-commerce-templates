# Fitness Vertical (not yet implemented)

Placeholder — to be built next, following the pattern established in `verticals/education/`. Per the architecture spec (§5.2), classes model as bookable/configurable Commerce products with date/time custom options and memberships as subscription products; expect a class-schedule-grid PLP variant and no real-time capacity locking (documented as a limitation, not solved).
