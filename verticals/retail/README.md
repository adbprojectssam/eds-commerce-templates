# Retail Vertical (not yet implemented)

Placeholder. Per the architecture spec (§5.1), retail is the baseline vertical — the shared blocks/scripts/styles at the repo root map onto it directly with no vertical-specific blocks needed. To implement: add `config.json` (point at the real or demo Commerce instance) and `drafts/` content here, following the pattern in `verticals/education/`.
